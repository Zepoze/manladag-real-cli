<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title><%= (manga && chapter) ? `${manga} n°${chapter}`:'Page Title' %></title>
        <meta name="viewport" content="width=device-width, initial-scale=1">
    </head>
    <style>
    body {
        background-color: #222222
    }
    .img-responsive {
        height:auto;
        max-width: 100%;
        margin: 30px 0
    }
    #wrap-img {
        text-align: center
    }
    </style>
    <body>
        <div id="wrap-img">
            <% files.forEach(e => {%>
                <img src="<%= e %>" class="img-responsive"/>
            <% }) %>
        </div>
    </body>
</html>